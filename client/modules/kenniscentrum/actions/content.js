export default {
    getFolderContent({LocalState, Meteor, Store}, folderName) {
        //LocalState.set('FOLDER_CONTENTS_REQUEST', true)
        //Meteor.call('_kenniscentrum.loadFolderContents', (err, data) => {
        //    if (err) alert('Error loading file contents')
        //
        //    LocalState.set('FOLDER_CONTENTS_RESPONE', data)
        //
        //})
        //LocalState.set('FOLDER_CONTENTS_REQUEST', false)

    }
}